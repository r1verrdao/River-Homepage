"use client";

// Chakra UI removed — portfolio uses vanilla CSS + inline styles.
// This wrapper is kept for future use if needed.
export function Providers({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
